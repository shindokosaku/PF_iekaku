<% if resource.errors.any? %>
  <div class="error_messages">
    <h2><%= pluralize(resource.errors.count, "エラー") %>が発生しました:</h2>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>