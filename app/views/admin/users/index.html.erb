<h1 class="mt-4 mb-5">ユーザー一覧</h1>

<% @users.each do |user| %>
  <div class="user-item border-bottom mb-3 pb-2">
    <h3>
      <%= link_to user.nickname, admin_user_path(user) %>
    </h3>
    <p>
      <%= user.email %>
    </p>
  </div>
<% end %>
